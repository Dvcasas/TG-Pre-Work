/* Conditional operators are logic gates that function to manipulate the flow
of the program based on certain requirements. The basic conditional statements
are if, elseif, else and switch. If the condition is true the logic will run
otherwise it will skip over it. */
