let mood = 'sleepy';
let tirednessLevel = 6;
if (mood === 'sleepy' && tirednessLevel > 8) {
  console.log('time to sleep')
} else {
  console.log('not bed time yet')
}

/* Logical operators are used to add additional conditions. In the case of the
and operator, both conditions must match true in order to evaluate as true.
The or operator only requires one condition to be met. The not operator inverts
the condition result for a boolean. */
