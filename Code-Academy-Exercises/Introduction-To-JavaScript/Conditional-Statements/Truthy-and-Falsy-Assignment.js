let tool = '';
tool = 'marker';

// Use short circuit evaluation to assign writingUtensil variable below:
let writingUtensil = tool || 'pen';

console.log(`The ${writingUtensil} is mightier than the sword.`);

/* A short-circuit evaluation allows a declaration to check in sequence the
assignment of multiple values in the event one is falsy and preceded. */
