let sale = true;
sale = false;
if (sale) {
  console.log('Time to buy!');
}

/* The if keyword is used to determine if a condition is true. If the condition
matches true, the code enclosed in the if statment will run. */
