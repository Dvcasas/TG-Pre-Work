let hungerLevel = 7;
if (hungerLevel >= 7) {
  console.log('Time to eat!')
} else {
  console.log('We can eat later!')
}

/* Comparison operators are used to evaluate a condition in comparison to the
given value. The condition will match either true or false based on the result
of the comparison. */
