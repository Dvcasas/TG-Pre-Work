class Surgeon {
  constructor(name, department) {
    this._name = name;
    this._department = department;
  }
}

/* The constructor is a method that creates the object. It is automatically
invoked when instantiate the object. */
