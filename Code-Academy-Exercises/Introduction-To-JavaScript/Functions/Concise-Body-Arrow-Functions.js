const plantNeedsWater = day => day === 'Wednesday' ? true : false;

/* Arrow functions can be made even further concise by using a certain syntax.
This would be the variable declaration and assignment operator followed by the
parameters, the 'fat arrow' and the code block. There is no need for a return
statement since it is implicit and will place the value in the variable. */
