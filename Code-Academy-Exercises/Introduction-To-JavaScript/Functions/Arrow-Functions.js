const plantNeedsWater = (day) => {
  if (day === 'Wednesday') {
    return true;
  } else {
    return false;
  }
};

/* The arrow function warks the same as an function expression only with a
different syntax. In this case the you write the parameters following a 'fat
arrow' and then the code block. */
