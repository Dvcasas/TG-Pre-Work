const vacationSpots = ['Bali', 'Paris', 'Tulum'];

// Write your code below
for (let i = 0; i < vacationSpots.length; i++ ){
  console.log('I would love to visit ' + vacationSpots[i]);
}

/* It is possible to iterate through an array using a for loop. You can use the
counter to select am index. As the loop progresses through each iteration, so
will the position in the array. */
