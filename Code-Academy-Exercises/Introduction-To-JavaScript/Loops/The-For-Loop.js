// Write your code below
for (let counter = 5; counter < 11; counter++) {
  console.log(counter);
}

/* A for loop is typically used when you know exactly how many times to iterate
through the loop. It requires the iterator, the stopping condition and the
iteration statement. */
