const objectives = ['Learn a new languages', 'Read 52 books', 'Run a marathon'];

console.log(objectives.length);

/* The length of the array does not shift down the way the index does. When
using the length property, it will return the number of items in the array. */
