const hobbies = ['kayaking', 'snowboarding', 'napping'];
console.log(hobbies);

/* An array is represented by [] and each element can hold data. You can
seperate the elements with commas. In the example above, the array contains 3
elements of string data. */
