const chores = ['wash dishes', 'do laundry', 'take out trash'];

chores.push('cook dinner', 'mop floor');
console.log(chores);

/* There are methods built into the array class to append elements to an array.
The push() method will add elements to the end of an array. This is considered
a destructive array method since it changes the initial array. */
