const numberClusters = [[1, 2], [3, 4], [5, 6]];
const target = numberClusters[2][1];

/* It is possible to nest arrays with an array. Almost a 2 dimensional array
but rather a specific index will have an alternate dimension. Each value in
a nested array must be referenced using each dimension of the array. */
