const logVisibleLightWaves = () => {
  const lightWaves = 'Moonlight';
  console.log(lightWaves);
};

logVisibleLightWaves();
// console.log(lightWaves);

/* Block scope indicates a variable that is declared within the curly braces and
is only accessible by code within that block */
