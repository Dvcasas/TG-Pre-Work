let myName = 'Derek';
let myCity = 'Edinburg';
console.log(`My name is ${myName}. My favorite city is ${myCity}.`);

/* You can interpolate variables into a string literal by using the ` ` symbol
and inserting the variable name in ${var}. This makes inserting variables into
strings easier than concatenation. */
