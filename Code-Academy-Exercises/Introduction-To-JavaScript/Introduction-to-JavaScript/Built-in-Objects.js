console.log(Math.floor(Math.random() * 100))
console.log(Math.ceil(43.8))
console.log(Number.isInteger(2017))

/* There are a number of built in objects in javascript. You can use these to
These are generally expected features to any programming language and provide
greater flexibility when using the language. It also helps the developer avoid
having to code in the universally expected classes. */
