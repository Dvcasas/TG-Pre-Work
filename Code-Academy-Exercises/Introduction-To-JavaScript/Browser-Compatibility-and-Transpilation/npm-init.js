var pasta = "Spaghetti"; // ES5 syntax

const meat = "Pancetta"; // ES6 syntax

let sauce = "Eggs and cheese"; // ES6 syntax

// Template literals, like the one below, were introduced in ES6
const carbonara = `You can make carbonara with ${pasta}, ${meat}, and a sauce made with ${sauce}.`;

/*
Had issues with this example. The terminal would not properly load but I get
the idea.

In this example, the npm init command is demonstrated to create a json file
conatining the metadata for the converted project. Through it is not required
to fill in the propmpts it is recommended to differentiate it from other builds. 
*/
