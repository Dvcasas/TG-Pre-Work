var pasta = "Spaghetti"; // ES5 syntax

const meat = "Pancetta"; // ES6 syntax

let sauce = "Eggs and cheese"; // ES6 syntax

// Template literals, like the one below, were introduced in ES6
const carbonara = `You can make carbonara with ${pasta}, ${meat}, and a sauce made with ${sauce}.`;

/*
The terminal won't function for me... Not in Chrome or edge. :(

This example primarily demonstrates how to add packages to the devDependencies.
Once the dependencies are installed, other developers can run the having to
install the dependencies themselves. 
*/
