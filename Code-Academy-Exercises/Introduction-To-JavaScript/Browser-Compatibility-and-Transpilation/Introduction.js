var pasta = "Spaghetti"; // ES5 syntax

const meat = "Pancetta"; // ES6 syntax

let sauce = "Eggs and cheese"; // ES6 syntax

// Template literals, like the one below, were introduced in ES6
const carbonara = `You can make carbonara with ${pasta}, ${meat}, and a sauce made with ${sauce}.`;

/* Because of broswer updates and new versions of javascript being released,
browsers may have issues running some forms of javascript. There are references
and tools available to check and convert code to older versions to see if it
will run in all browsers. Using the newest conventions is usually prefered but
not always widely supported. */
