/* In this next section I'll be looking into GET requests using fetch(). This
method contains relevant information the API needs, sends that request to the
API, and returns a promise with the information the API sent back. */
